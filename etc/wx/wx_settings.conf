# ============================================================
# WX-Wireless 配置文件
# 路径: /etc/wx/wx_settings.conf
# 修改后无需重启，脚本会自动加载
# ============================================================

# === WxPusher 微信推送配置 ===
# 获取方式: https://wxpusher.zjiecode.com/admin/login
WXPUSHER_ENABLED=0                    # 推送开关 (1=开启, 0=关闭)
WX_APP_TOKEN="输入自己的"  # 应用Token
WX_MY_UID="输入自己的"       # 用户UID
WXPUSHER_API_URL="https://wxpusher.zjiecode.com/api/send/message"

# === 网络检测配置 ===
PING_TARGET="223.5.5.5"               # 网络连通性检测IP

# === 自动切换配置 ===
AUTO_SWITCH_MAX_RETRIES=25            # 连接/检测最大重试次数
RETRY_WIFI_INTERVAL=688               # 相同热点重试冷却时间 (秒)
RESTART_THRESHOLD=10                  # 累计失败后自动重启阈值

